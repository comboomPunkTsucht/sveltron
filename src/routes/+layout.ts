// Deaktiviert Server-Side-Rendering
export const ssr = false;
// Rendert die Basis-HTML als statische Datei (die index.html für unseren Fallback)
export const prerender = true;
// Hilft SvelteKit's internem Router im file:// Protokoll
export const trailingSlash = "always";
